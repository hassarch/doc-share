/**
 * Common module — shared DTOs, exception types, and stateless utilities
 * used across other modules.
 *
 * <p>Deliberately does NOT contain business logic, entities, or
 * repositories — those belong to a specific domain module. If you're
 * tempted to put a service class here, it probably belongs in a domain
 * module instead.
 */
package com.docshare.backend.common;
