/**
 * Cross-cutting Spring configuration: security filter chain, CORS, Kafka
 * producer/consumer config, Redis config, OpenAPI, correlation-ID filter.
 *
 * <p>Configuration that is specific to a single module (e.g. an auth-only
 * bean) lives in that module instead of here.
 */
package com.docshare.backend.config;
